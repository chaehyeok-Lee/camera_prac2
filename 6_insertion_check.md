# 6. 삽입 불량 판단

## 목적
나사 삽입 상태를 3가지로 판단: 정상(다박힘) / 덜박힘 / 틀어짐.

## 진행 계획
1. **틀어짐 - 각도**: RGB에서 나사머리 투영 모양의 장단축비(aspect_ratio) — 기울면 원이 타원으로 보임
2. **틀어짐 - 평면**: 나사머리-스터드홀 중심 (x,y) 거리가 허용치 초과
3. **덜박힘**: 나사머리 depth가 주변 패널 표면보다 허용치 이상 튀어나옴 — depth 필수, RGB만으론 불가
4. 하나라도 걸리면 해당 불량, 전부 정상이면 정상

## 상태
틀어짐(각도)·덜박힘(임계값 잠정)은 실캡처로 정상 동작 확인. 틀어짐(평면)은 실캡처 검증 중 "나사가 자기 구멍을 가려 매칭 불가"라는 근본 결함을 발견해 오탐은 막았지만 실질적 판정 방법은 아직 없음(재설계 필요, 다음 과제로 넘김)

---

### 틀어짐 - 각도 (RGB aspect_ratio) &nbsp;&nbsp;`검토중` (1차 구현/검증 완료, 임계값 근거 약함)
- [x] 나사머리 블롭의 2차 모멘트 공분산행렬 고유값비로 회전-불변 장단축비(aspect_ratio) 계산
- [x] aspect_ratio >= 1.5 → 틀어짐 (1.3에서 완화 - 아래 참고), 상한 2.5 넘으면 노이즈(배경 반사 등)로 배제
- [x] 실제 캡처로 검증: 육안상 기울어진 나사(ar=1.86) 정확히 분류, 정상 나사(ar=1.1~1.3)와 구분

**결론**: RGB만으로 학습 없이 틀어짐(각도) 탐지 가능 확인. 구현은 `scripts/5_px_to_mm.py`의 `detect_screw_heads_by_color`/`blob_aspect_ratio`에 있음 (5번 지름 측정 중 나사 검출하다 발견되어 그쪽 파일에 위치, 문서는 여기로 이전).

**조사**: 원판이 카메라 광축 대비 각도 θ만큼 기울면 투영 시 장축은 그대로, 단축만 cos(θ)배로 줄어듦 → `aspect_ratio = 1/cos(θ)`, 역으로 `θ = arccos(1/aspect_ratio)`. ar=1.86 → θ≈57.5°. 절대각도 신뢰도는 낮음(정면 기준 카메라 배치·원근 왜곡 등 단순화 가정) — 하지만 상대 비교(이 나사가 저 나사보다 더 기울었다)는 유효해서, 이진 분류보다 **연속값(기울기 정도)으로 등급 매기는 데 활용 가능**.

**수정(2026-08-20)**: 실시간 필드 테스트에서 나사 3개 중 2개가 틀어짐으로 판정(ar=1.34, 1.72) - 사용자 확인 결과 1.34는 오탐으로 판단돼 임계값을 1.3→1.5로 완화. 1.72는 그대로 틀어짐 유지(명확히 큰 값).

**보완 필요**: (1) 임계값(ar=1.5)이 여전히 표본이 적어 정해짐 — 정상/틀어짐 나사 여러 개로 재검증 필요. (2) 기울어진 나사는 foreshortening으로 투영 면적이 작아 보여 5번의 diameter_mm이 과소추정됨 — 틀어짐으로 분류된 인스턴스는 지름 실측 검증에서 제외 권장. (3) 배경(모니터 스탠드 반사물)이 나사로 오탐되며 aspect_ratio=2.32로 틀어짐까지 판정된 사례 발견(diameter 오차 +138%) - depth 범위 필터를 통과한 걸로 봐서 우연히 시편과 비슷한 거리에 있던 것으로 추정, 별도 조치 필요(면적 상한 등).

### 틀어짐 - 평면 (중심 좌표 비교) &nbsp;&nbsp;`검토중` (실캡처 검증 중 근본 결함 발견 → 판정보류 방식으로 수정, 실질적으로는 아직 판정 불가능)
- [x] 나사머리 중심과 짝이 되는 스터드홀 중심 매칭 (최근접 거리 기준, `match_nearest_stud_hole`)
- [x] (x,y) 오차를 px→mm 환산(depth+fx) 후 허용치와 비교
- [x] 실측 검증 → **결함 발견**: 정상 나사 3개가 전부 "틀어짐"으로 오판됨 (아래 참고)
- [x] 오탐 방지 수정: 매칭 거리가 비현실적으로 크면 판정 자체를 보류하도록 변경
- [ ] 실제로 판정이 되는 방법 자체는 아직 없음 (미해결 — 아래 보완 필요 참고)

**결론**: 허용치는 통계 추정이 아니라 **기하학적 제약**으로 정함 — `CENTER_OFFSET_THRESHOLD_MM = (stud_hole_mm - screw_head_mm) / 2` (지금 시편 기준 (10-6)/2=2mm). 이 값 자체은 원리적으로 타당하지만, **실캡처로 확인한 결과 이 임계값을 적용할 "매칭"이 애초에 불가능함**을 발견함(아래 조사).

**조사(결함)**: 제대로 삽입된 나사는 자기 구멍을 시각적으로 거의 다 가려서, RGB에서 그 구멍이 "빈 stud_hole"로 따로 검출되지 않음. 그 결과 `match_nearest_stud_hole`이 몇 칸 떨어진 **다른** 빈 구멍에 억지로 매칭되어 26~33mm짜리 가짜 오차를 만듦 — 정상 나사 3개가 전부 틀어짐으로 오판되는 걸 실측으로 확인(`results/6_insertion_check_result.json` 1차 실행분). 매칭 거리가 `CENTER_OFFSET_THRESHOLD_MM+3mm`를 넘으면 "판정보류"로 처리하도록 고쳐서 오탐은 막았지만(재검증 완료 — 이후 실행에서 정상 2개/틀어짐 1개(각도 기준)로 정상화), **이 방식으로는 사실상 항상 판정보류만 나옴** — 진짜 미해결 상태.

**보완 필요**: 이 방법 자체를 다시 설계해야 함. 후보: (1) 시편에 나사가 하나도 없는 상태로 한 번 캡처해서 전체 구멍 위치를 기준 좌표로 저장해두고, 이후 캡처의 나사 중심을 그 기준 좌표와 비교(카메라/시편 고정 전제 필요 — 지금처럼 손으로 들고 촬영하면 못 씀). (2) 나사머리-구멍 틈새(2mm 링)를 더 잘 잡도록 라벨/모델을 보강(현재 모델이 빈 구멍 위주로 학습되어 나사에 살짝 가려진 링은 아예 못 잡는 것으로 추정). 둘 다 지금 범위(시편 하나, 즉석 촬영) 밖이라 여기서는 보류만 해두고 다음 과제로 넘김.

### 덜박힘 (깊이 오차) &nbsp;&nbsp;`검토중` (foam_panel_v1 임계값 확정, 새 시편은 여전히 미확정)
- [x] 나사머리 마스크의 depth vs 주변 패널 표면 기준 depth(평면 피팅) 비교 (`local_panel_depth_mm`)
- [x] 임계값 설정 방식 마련 — 진짜 양성 샘플이 없어 절대치 대신 **정상 판정 나사들의 protrusion_mm 분포를 실행마다 파일(`results/6_protrusion_baseline.json`)에 누적**, 표본 5개 이상 쌓이면 평균+3표준편차를 잠정 기준으로 사용
- [x] **2026-08-20 확정(사용자 결정)**: 누적 표본 53개를 확인한 결과 ~2~4.7mm(37개)와 ~7.4~9.5mm(16개)로 뚜렷이 갈리고 그 사이(4.74~7.44mm)엔 값이 하나도 없어서, `foam_panel_v1` 프로파일 전용 고정 임계값을 **5.0mm**로 확정(`detection_core.py`의 `SPECIMEN_PROFILES["foam_panel_v1"]["protrusion_threshold_mm"]`). 통계적 평균+3표준편차(약 15~16mm)는 이 시편 특유의 이중분포 때문에 지나치게 느슨해 사실상 항상 "정상"만 나오는 문제가 있었음 — 고정값으로 교체해 해결
- [ ] 새 시편(`tread_v2` 등)은 아직 표본이 없어 `protrusion_threshold_mm=None` — 통계적 폴백으로 동작 중, 표본 쌓이면 재검토

**결정 근거의 한계**: 53개 표본이 서로 다른 53개의 나사가 아니라 **같은 시편의 나사 몇 개(3~5개)를 여러 번 반복 측정한 값**임 — 즉 "~9mm대는 진짜 덜박힘"이라는 물리적 확증(캘리퍼 실측이나 육안 비교)은 아직 없고, 어디까지나 관측된 데이터의 빈 구간(gap)에 기반한 상대적 판단. 추후 실제로 덜박힘 시편이 확보되면 재검증 필요.

**조사**: 이 시편은 3번 과제(depth 전처리)에서 이미 확인했듯 **평평한 판이 아니라 리브/타일 구조**라 표면 depth가 위치마다 자연히 들쭉날쭉함. 따라서 "패널 전체 평균 depth"를 기준으로 삼으면 구조 자체의 높낮이 때문에 오탐/미탐이 날 수 있음. 단순 평균/최소분산 패치 두 방식 모두 시편이 카메라 대비 기울어져 있어(경사) 나사에서 조금만 떨어져도 depth가 달라지는 걸 "튀어나옴"으로 오판(둘 다 8mm로 동일하게 틀려서 발견)했음 — **최소자승 평면 피팅**으로 경사 자체를 모델링해서 나사 중심 위치의 "경사만 반영한 기대값"과 실측값을 비교하는 방식으로 교체, 경사와 진짜 돌출을 분리함. 구현은 `local_panel_depth_mm`.

**보완 필요**: 덜박힘 임계값이 아직 표본 부족으로 미확정 상태일 수 있음(파일 확인 필요) — 어차피 진짜 불량 샘플이 없어 이 잠정치는 "정상 나사끼리의 오차 범위"일 뿐, 실제 불량 판별력은 검증 전까지 신뢰하면 안 됨.

### 디버깅: 검출 후보가 있는데 결과가 0개로 나온 문제 &nbsp;&nbsp;`검토 완료` (원인 확인 완료, 근본 해결은 아님)
이전 실행에서 `screw_head 후보: 6개`가 찍혔는데 시각화 이미지엔 원/라벨이 하나도 안 그려짐 — `results` 리스트가 최종적으로 비어있었다는 뜻인데, 그 이유가 `build_instance`/`local_panel_depth_mm` 내부의 모든 실패 지점이 사유 없이 조용히 `continue`되도록 짜여있어 알 수 없었음.

**수정**: (1) `build_instance`에 `debug_label` 인자 추가 — 제외될 때마다 사유(마스크 비어있음/유효 depth 없음/범위 밖)를 콘솔에 출력. (2) 마스크 자체에 유효 depth가 하나도 없으면 마스크를 점진적으로 팽창(dilate)시켜 주변 유효 depth라도 확보하는 폴백 추가(단 `depth_source="dilated_mask"`로 표시해 정확도 낮음을 구분). (3) `local_panel_depth_mm`도 평면 피팅 샘플 부족 시 사유 출력하도록 추가.

**결론(재캡처로 확인됨)**: 로그 추가 후 재실행 결과 가설이 맞았음 — 4개 후보 중 1개가 정확히 `유효 depth 없음 (반사로 인한 홀 - 팽창해도 복구 안됨)` 사유로 제외됨(나머지 3개는 정상 처리). 반사가 강한 나사머리는 depth 홀이 팽창 폴백(5회, 최대 ~5px)으로도 못 채울 만큼 큼.

**보완 필요**: 팽창 반복 횟수/커널을 늘려 복구율을 높일 수 있으나, 늘릴수록 "나사 자체가 아닌 주변부 depth"로 대체되는 범위가 커져 protrusion/틀어짐 계산 정확도가 떨어지는 트레이드오프가 있음 — 지금은 손대지 않고 로그로 빈도만 추적 중.
