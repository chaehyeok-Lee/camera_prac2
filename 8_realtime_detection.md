# 8. 실시간 탐지 (계속)

## 목적
7번(실시간 삽입 불량 검출)의 후속 작업 기록. `7_realtime_detect.md`는 그대로 두고(기존 기록 보존),
이후 작업은 여기에 이어서 기록한다.

## 배경
7번에서 정지-트리거 상태머신(WAITING→SETTLING→MEASURING→RESULT_SHOWN) 현장 검증 완료, 측정
소요시간 -37% 개선, 원 피팅 정확도 합성테스트 검증까지 끝난 상태에서 이어서 진행.

## 상태
틀어짐 완화+라이브 오버레이 실카메라 검증 완료(이동 추적까지 확인). 반실시간/실시간 셀프
리뷰로 구체적 개선점 정리(프로파일링 근거 포함) - 아직 미적용.

---

### 틀어짐 임계값 완화 + 이동 중 라이브 오버레이 &nbsp;&nbsp;`검토 완료` (실카메라 검증)
- [x] `detect_screw_heads_by_color` 틀어짐 aspect_ratio 임계값 1.3→1.5 완화 — 필드 테스트에서 ar=1.34가 오탐(사용자 확인), ar=1.72는 진짜 틀어짐이라 유지
- [x] `detect_live()`/`render_live_overlay()` 추가 — WAITING/SETTLING(시편 이동 중)에도 단일 프레임 기준 라이브 검출 오버레이 표시 (mm은 참고용, baseline 미반영)
- [x] 무거운 검출(YOLO+평면보정)은 `LIVE_DETECT_EVERY_N_FRAMES=5`마다만 갱신 — 모션 diff 계산 루프 속도(SETTLE_FRAMES 타이밍) 보호
- [x] 실카메라 재실행 검증: 라이브 오버레이 켠 상태로도 상태전이 정상(1.71초, 오히려 더 빠름), 틀어짐 판정 2정상/1틀어짐으로 정상화

**결론**: 시편이 움직이는 동안에도 화면에 검출 결과가 실시간으로 보이면서, 기존 정지-트리거 정밀측정 흐름(정확도)은 그대로 유지됨을 실측으로 확인.

**보완 필요**: 배경(모니터 스탠드 반사물)이 나사로 오탐+틀어짐 판정된 사례가 한 번 있었음(ar=2.32, 지름오차 +138%, `6_insertion_check.md` 참고) — 이번 재실행에선 재현 안 됐으나 면적/컨텍스트 기반 필터 추가 검토 필요, 후순위로 미룸.

---

### 라이브 오버레이 증거 캡처 &nbsp;&nbsp;`검토중` (동작 확인, 이동 중 추적은 미확인)
사용자가 "진짜 실시간이 구현됐냐"고 재확인 요청 - `--once` 데모는 정지 상태에서 0.5초 만에 MEASURING으로 넘어가버려 라이브 오버레이 화면을 실제로 보여주지 못했음(화면에 순간 떴다 사라질 뿐 저장도 안 됨).

- [x] `--live-demo-sec N` 옵션 추가 - 정지판정 없이 라이브 검출 갱신 프레임을 그대로 `results/8_live_demo/`에 저장
- [x] 6초 실행(정지 상태), 16장 저장 확인 - 매 갱신 130~160ms, screw=3/hole=13 일관되게 검출
- [x] 8초 재실행(사용자가 실제로 시편을 손으로 움직임) - 19장 저장, screw 검출개수가 2~7개로 프레임마다 계속 바뀜(각도/가림 변화 반영) - t=0.5s/4.3s/8.0s 프레임 비교로 손 위치·나사 배치가 실제로 달라지는 것을 육안 확인

**결론**: 라이브 검출이 정지 트리거와 무관하게 계속 돌 뿐 아니라, **실제로 움직이는 시편을 따라 검출이 갱신되는 것까지 실측으로 증명됨**.

---

### 셀프 리뷰: 반실시간(정지-트리거) vs 실시간(라이브) 개선점 &nbsp;&nbsp;`검토중` (분석 완료, 미적용)
사용자 요청으로 두 모드를 각각 리뷰 - 프로파일링 근거로 구체화.

**반실시간(MEASURING, 정지-트리거 정밀측정)**
- [ ] 두 버스트 불일치 판정이 "개수만 비교"라 다소 거침 - 위치 기반 매칭으로 정교화 가능(복잡도 대비 이득 불확실, 우선순위 낮음)
- [ ] `POST_MEASURE_COOLDOWN_FRAMES` 버그 수정, 진짜 움직임으로는 아직 재검증 안 됨
- [ ] `MOTION_DIFF_THRESHOLD_MM=0.5mm`가 여전히 잠정치 - 원격이라 실패했던 캘리브레이션 아직 미해결
- [ ] "시편 없음" 상태 처리 없음 - 빈 배경도 노이즈 낮으면 측정이 트리거될 수 있음(낭비이나 치명적이진 않음)

**실시간(라이브 오버레이)** - `detect_live()` 프로파일링(YOLO 44ms + stud_hole 원피팅루프 ~54ms + 색상검출 55ms = 154ms, 실측 130~180ms와 일치)으로 확인:
- [ ] **라이브 경로에서 원 피팅(호모그래피 보정) 생략 검토** - 정밀도는 정지 후 MEASURING에서만 필요, 라이브는 근사치(`build_instance`)로 충분 → 구멍당 ~4ms×13개=54ms 절감 추정(154ms→~100ms), 갱신 주기 6Hz→10Hz급 개선 가능
- [ ] 색상검출(55ms)이 예상보다 무거움 - 이전 벤치 7ms는 더 단순한 프레임 기준이었음, 라이브용 경량화 검토 여지
- [ ] 검출 개수가 프레임마다 2~7개로 요동침(각도/가림 변화) - 사람이 보기엔 깜빡임처럼 느껴질 수 있음, 프레임간 위치매칭+짧은 지속성(2~3프레임 유지)으로 안정화하면 체감 개선 여지
- [ ] 빠른 움직임에서 모션블러로 인한 오탐(7개까지 튐) 확인 - 셔터/노출 튜닝 여지, 범위 밖일 수 있음
- [ ] `LIVE_DETECT_EVERY_N_FRAMES=5`가 실제 diff-loop 프레임레이트/SETTLE_FRAMES 타이밍에 미치는 영향을 직접 측정 안 함

**결론**: 반실시간은 안정적이나 모션 임계값이 미확정 상태로 남아있는 게 가장 큰 리스크. 실시간(라이브)은 원 피팅 생략이 가장 확실한 성능 개선 레버로 프로파일링까지 마쳤음 - 다음에 적용 지시하면 바로 구현 가능.
