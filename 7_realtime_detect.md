# 7. 실시간 삽입 불량 검출

## 목적
카메라 고정 + 시편 이동 전제로, 정지 감지 시 자동으로 5/6번 판정 파이프라인을 실행하는 실시간 버전.

## 배경
정적(1샷) 파이프라인은 GitHub `v1-static` 태그로 백업 완료. 3인 리뷰로 설계 검증.

## 진행 방식
1. `detection_core.py`로 검출/mm환산/평면보정 로직 통합 (5/6/7번 공용, 3중 복붙 방지)
2. 리팩터링 전후 출력 동일성 회귀 테스트 (`test_detection_core_regression.py`)
3. `7_realtime_detect.py` 상태머신(WAITING→SETTLING→MEASURING→RESULT_SHOWN) 구현
4. `calibrate_motion_threshold.py`로 모션 임계값을 감이 아니라 실측(max-gap)으로 산출

## 상태
전체 상태전이 현장 검증 완료(위와 동일) + 이동 중 라이브 검출 오버레이 추가, 틀어짐 임계값
완화(1.3→1.5, 필드 오탐 확인 후 반영) 실측 검증 완료. 남은 건 모션 임계값 정밀 재보정.

---

### 공용 모듈 분리 &nbsp;&nbsp;`검토 완료` (회귀 테스트로 동일 출력 확인)
- [x] `detection_core.py` 생성 — `5_px_to_mm.py`의 검출/build_instance/평면-호모그래피 보정 로직 이전
- [x] `5_px_to_mm.py`, `6_insertion_check.py`가 `detection_core`를 import하도록 변경 (기존 `import_module("5_px_to_mm")` 방식 대신 직접 import)
- [x] `4-2_hole_detection_experiments.py`도 새 모듈 참조로 수정 (안 하면 깨지는 걸 발견해 반영)
- [x] `6_insertion_check.py`의 판정 로직을 `classify_insertion()` 함수로 추출 (7번이 그대로 재사용)
- [x] 리팩터링 전/후 출력 비교 회귀 테스트 작성 및 통과 (`test_detection_core_regression.py`, 합성 depth + 실제 held-out 컬러 이미지 사용)

**결론**: 5/6번의 실제 동작(main() 결과)은 리팩터링 전후로 완전히 동일함을 회귀 테스트로 확인. 카메라 없이도 검증 가능한 부분은 여기까지 끝냄.

---

### 실시간 상태머신 &nbsp;&nbsp;`검토 완료` (현장 실카메라로 전체 순환 3회 검증)
- [x] WAITING/SETTLING/MEASURING/RESULT_SHOWN 상태머신 구현 (`7_realtime_detect.py`)
- [x] depth 프레임간 ROI 평균 절대차로 모션 감지 (color diff 대신 - 조명 변화에 안 흔들림)
- [x] MEASURING에서 15프레임 버스트 2회 자기검증 → 불일치 시 기존 검증된 30프레임으로 재측정
- [x] YOLO CPU 추론 41ms/색상검출 7ms 실측 (카메라 없이 기존 학습 이미지로 벤치마크 가능했음)
- [x] `--once` 옵션 추가(첫 측정 후 자동 종료) + 결과 json/png 저장 - 원격 환경(GUI에 q 입력 불가)에서도 검증 가능하게 함
- [x] 실카메라 1차 실행: WAITING→SETTLING→MEASURING 정상 전이, 자기검증이 실제로 두 버스트 불일치(screw 4개/3개)를 잡아내 30프레임 폴백 정상 동작, 결과가 정적 6번과 동일 수준(나사 2정상/1틀어짐, 구멍 13개) - 5.3초 소요
- [x] MEASURING↔RESULT_SHOWN 전환 시 temporal filter 상태 흔들림으로 재측정 루프 도는 버그 발견 → `POST_MEASURE_COOLDOWN_FRAMES`(20프레임 유예) 추가로 수정(실측 검증은 아직 - 시편을 움직여봐야 확인됨)
- [x] 현장에서 시편을 실제로 치웠다 놓으며 3회전 테스트 (2026-08-20): WAITING↔SETTLING↔MEASURING↔RESULT_SHOWN↔(모션 재감지)↔WAITING 전체 순환 3번 다 정상 동작, `POST_MEASURE_COOLDOWN_FRAMES` 수정으로 고친 재측정 루프 버그도 재발 안 함
- [x] 자기검증(15프레임 버스트 2회) 실동작 확인: 2회차에서 실제로 불일치(screw 4개/3개) 발생 → 30프레임 재측정 자동 전환 → 사유 로그(반사로 depth 없음, depth=136mm 배경 오탐 추정 - 시편 옮기던 손으로 추정)까지 정확히 출력됨
- [ ] `calibrate_motion_threshold.py`로 모션 임계값(현재 0.16mm 노이즈 상한 기준 잠정 0.5mm) 정밀 재산출 - 이번 테스트에서 0.5mm 자체는 3회전 다 문제없이 동작했으나 "진짜 최적값"은 아직 근거가 약함
- [ ] SETTLE_FRAMES/DEBOUNCE_FRAMES 체감 튜닝

**결론**: 상태머신 전체(모션감지→정지판정→측정→결과표시→재트리거)가 실카메라+실제 시편 이동으로 검증 완료. MEASURING 경로도 정적 파이프라인과 동등한 품질 재확인. 자기검증 메커니즘이 실사용 중 실제 불일치를 잡아내고 정확한 사유까지 로그로 남기는 것까지 확인돼, 설계가 실제로 유효함이 증명됨.

**설계상 제약**: RealSense 파이프라인은 단일 스레드에서만 안전해서, MEASURING 중엔 라이브 프리뷰가 갱신되지 않고(마지막 화면 유지) 측정 끝나면 결과 화면으로 전환됨 — "완전히 끊김없는" 실시간은 아님. 정지-트리거 설계상 자연스러운 트레이드오프로 판단해 그대로 둠.

**보완 필요**: `calibrate_motion_threshold.py`를 이제 시편을 실제로 움직이며 재실행해 0.5mm 잠정값을 정식 값으로 교체 필요. SETTLE_FRAMES(15프레임, ~0.5초)/DEBOUNCE_FRAMES 체감 튜닝도 남음.

---

### 셀프 리뷰: 정확성/실시간성 개선 &nbsp;&nbsp;`검토 완료` (실측·합성테스트로 검증됨)
- [x] (실시간) 측정 구간 타이밍 계측 추가 → 첫 detect 호출만 1.85초(YOLO 최초 추론 초기화 비용), 이후 0.12초로 확인
- [x] (실시간) 모델 워밍업을 대기 상태 진입 전(시작 시)으로 이동 - 사용자가 기다리는 순간이 아니라 프로그램 시작 시 1.3초 소비
- [x] (실시간) `capture_averaged_depth`의 기본 warmup=10이 정적 스크립트(콜드스타트) 기준값이라 실시간(계속 스트리밍 중)엔 과함을 확인 - 같은 MEASURING 사이클 내 재측정 버스트는 warmup=0으로 축소
- [x] (실시간) 실측 결과: 측정 소요시간 4.93초 → 3.08초(-37%)
- [x] (정확성) 합성 정답 데이터(알려진 기울기·지름의 3D 원을 핀홀 투영)로 `rectify_and_fit_circle`을 처음으로 정량 검증
- [x] (정확성) 틀어진 각도(0~50도)에서 오차가 커지는 걸 확인했지만, 반지름을 4배로 늘려 재검증한 결과 오차가 대부분 사라짐(2.47%→0.30%) → 알고리즘 결함이 아니라 실제 구멍이 작아서(반지름~11px) 생기는 픽셀 이산화 한계로 확인

**결론**: 실시간성은 병목(모델 초기화)을 정확히 찾아 사용자 체감 대기시간을 37% 줄임. 정확성은 "왜 얼마나 틀리는지" 정답 있는 데이터로 처음 확인 - 이전 방식(원형도 필터, +16~20% 오차)보다 지금 방식(평면-호모그래피 보정)이 실제로 훨씬 정확하고, 남은 오차(1~4%)는 코드로 못 줄이는 물리적 한계(해상도)임을 확인.

**보완 필요**: 해상도 한계를 넘으려면 카메라를 시편에 더 가까이 두거나(측정거리 250mm는 confirm.md 고정값이라 임의 변경 불가), 더 높은 해상도 스트림을 쓰는 방법뿐 - 지금 범위 밖.

---

### 이동 중 라이브 검출 + 틀어짐 임계값 완화 &nbsp;&nbsp;`검토 완료` (실카메라로 검증)
- [x] 틀어짐 aspect_ratio 임계값 1.3→1.5 완화 - 필드 테스트에서 ar=1.34짜리가 오탐(사용자 확인)으로 판단됨, ar=1.72는 명확한 틀어짐이라 그대로 유지
- [x] WAITING/SETTLING(시편 이동 중)에도 라이브 검출 오버레이 추가 - `detect_live()`/`render_live_overlay()` (단일 프레임 기준, mm 참고용, baseline 미반영)
- [x] 무거운 검출(YOLO+평면보정)은 `LIVE_DETECT_EVERY_N_FRAMES`(5)마다만 갱신 - 매 프레임 돌리면 모션 diff 루프가 느려져 SETTLE_FRAMES 타이밍이 틀어지는 걸 방지
- [x] 실카메라로 재실행: 라이브 오버레이 켠 상태로도 상태전이 정상 동작(오히려 1.71초로 더 빠름), 틀어짐 판정도 2정상/1틀어짐으로 정상화 확인

**결론**: 두 가지 다 실측으로 검증됨 - 시편이 움직이는 동안에도 화면에 검출 결과가 실시간으로 보이면서, 기존 정지-트리거 정밀측정 흐름은 그대로 유지됨.

**보완 필요**: 배경(모니터 스탠드 반사물)이 나사로 오탐되며 틀어짐까지 판정된 사례가 있었음(`6_insertion_check.md` 참고) - 면적/컨텍스트 기반 필터 추가 검토 필요하나 이번엔 재현 안 돼 후순위로 미룸.
