# 5. px to mm 지름 확인

## 목적
세그멘테이션 마스크의 나사머리/스터드홀 지름을 실제 mm 단위로 환산한다.

## 진행 방식
1. 마스크에서 픽셀 단위 등가원 지름 측정 (면적 기반, sqrt(4*area/pi))
2. 마스크 영역의 depth 중앙값 + 컬러 스트림 fx로 mm 변환 (핀홀 모델)
3. 캘리퍼 실측값(screw_head=6mm, stud_hole=10mm)과 비교해 오차 산출
4. 검출은 하이브리드: stud_hole=YOLO(배경과 색이 비슷해 학습 기반 필요), screw_head=색상(명도) 임계값(은색 vs 무광 검정 대비가 커서 학습 불필요)

## 상태
하이브리드 검출로 screw_head 정확도 크게 개선(오차 -1.6%) — stud_hole의 mm 과대추정(+16~20%)은 원인 미확정으로 남음

---

### 1차 실행 결과 &nbsp;&nbsp;`검토중` (중복 제거 완료, screw_head 색상기반 전환으로 대폭 개선)
- [x] 3번 depth 파이프라인 + 4번 YOLO 모델 결합 스크립트 작성
- [x] 캘리퍼 실측값 반영 (screw_head=6mm, stud_hole=10mm)
- [x] 실제 캡처로 1차 실행 및 시각화 확인
- [x] 중복 검출 제거: YOLO NMS iou 0.7→0.5 강화 + 중심거리 기반 후처리(같은 클래스, 중심거리 < 평균지름x0.6이면 confidence 낮은 쪽 제거)
- [x] screw_head를 YOLO에서 색상(명도, Otsu 임계값) 기반 검출로 교체 — 사용자 제안(은색 vs 검은 배경 대비로 학습 없이도 구별 가능)
- [x] stud_hole 검출개수 하락(13→9) 원인 진단: confidence 분포 직접 확인 후 threshold 0.35→0.28로 재조정
- [x] screw_head 정상/틀어짐(6번 케이스) 분류 추가 — 사용자 제안(기울어진 나사=타원으로 보임)을 모멘트 기반 장단축비(aspect_ratio)로 구현, 노이즈 배제용 상한(2.5)도 추가

**결론**: screw_head 색상 기반 전환 후 대부분 정확히 검출(오차 -1.6~-7.6%)로 YOLO 대비 압도적으로 개선(YOLO는 held-out에서 0개 검출). stud_hole 검출개수 하락은 다른 세션이 confidence threshold를 0.25→0.35로 과하게 올린 게 원인이었음 — 실측 confidence 분포 확인 결과 0.26~0.36 구간에 진짜 구멍이 더 있고 노이즈는 0.10 밑으로 뚝 떨어짐(경계는 0.28 근처) — 0.28로 재조정해 9→11개로 회복. screw_head 블롭의 2차 모멘트 공분산행렬 고유값비(rotation-invariant)로 aspect_ratio 계산 → 1.3 이상이면 "틀어짐"으로 분류, 시각화에서 빨강으로 구분. 실제 기울어진 나사(ar=1.86)를 정확히 잡고 정상 나사(ar=1.1~1.3)와 구분됨(육안 확인). 상한 2.5 넘는 블롭(ar=3.94, 배경 반사)은 노이즈로 배제.

**보완 필요**: (1) stud_hole의 +16~20% mm 과대추정 원인 미확정 — 유력 후보는 YOLO 마스크가 160x160 저해상도 프로토타입을 업샘플링+임계값(0.5)으로 만들어서 경계가 실제보다 1~2px 부풀려지는 현상(작은 객체일수록 상대오차 커짐). (2) 틀어짐 판정 임계값(ar=1.3)이 시편/샘플 1~2개로 정한 값이라 근거가 약함 — 실제 정상/틀어짐 나사를 더 많이 모아 임계값 검증 필요. (3) 기울어진 나사는 투영 면적이 실제보다 작아 보여(foreshortening) diameter_mm이 과소추정될 수 있음 — 아직 보정 안 함.

**추가(다른 세션)**: 정적 held-out 이미지로 오프라인 확인 중 색상 검출기가 폼 패널 틈새 배경(흰 벽)을 screw_head로 오탐하는 사례 발견 — `build_instance`에 `SPECIMEN_DEPTH_RANGE_MM=(180,400)` depth 범위 필터 추가해 배경을 제외하도록 함. 실제 카메라로 검증 안 됨 — 다음 실캡처 때 이 범위가 맞는지 같이 확인 필요.

**추가(6번 작업 중 발견)**: 6번에서 새 시편으로 재캡처 시 검출 후보는 있는데 최종 결과가 0개가 되는 문제가 있었음 — `build_instance`가 제외 사유를 조용히 삼켜서 원인 파악이 안 됐던 게 근본 원인. `debug_label` 인자로 제외 사유(마스크 비어있음/유효 depth 없음/범위 밖) 콘솔 출력 추가, 반사로 마스크 depth가 통째로 무효인 경우 마스크를 팽창(dilate)시켜 주변 유효 depth로 복구하는 폴백도 추가(`depth_source` 필드로 "mask"/"dilated_mask" 구분 — 후자는 나사 자체가 아닌 인접부 depth라 정확도 낮음에 유의). stud_hole 검출 로직은 `detect_stud_holes()`로 분리해 5번/6번 공용화.

**추가(stud_hole 재현율 개선, 4-2 실험)**: `detect_stud_holes()`의 confidence를 0.28→0.15로 낮추고 원형도(circularity≥0.7) 후처리 필터를 추가 — 같은 프레임 기준 9개→12~13개로 재현율 개선, 오탐 증가는 육안상 확인 안 됨. 실험 근거는 `4_segmentation.md`의 "stud_hole 탐지 재현율 개선 실험(4-2)" 참고.

**추가(평면-호모그래피 보정으로 재설계)**: 이미지 중심에서 멀어질수록(오프축) 원이 타원으로 왜곡되는 문제를 이진분기(가장자리냐 아니냐) 대신, 매 캡처마다 depth로 패널 3D 평면을 피팅해 호모그래피로 전체 이미지를 "정면에서 본 것처럼" 보정하는 연속적인 방식으로 재설계함(`fit_panel_plane_3d`+`build_rectification_homography`+`rectify_and_fit_circle`). 렌즈 왜곡계수는 실측 결과 0으로 확인 — 원인은 렌즈가 아니라 패널 자체의 기울어짐. 합성 데이터로는 지름 오차 3.1%→0.3%로 개선 검증됐으나, **실제 시편에서는 기존 +16~20% mm 오차가 거의 그대로(+16.0%) 남음** — 이 시편의 기울기가 크지 않아서로 추정, 즉 mm 오차의 주 원인은 패널 기울기가 아니라 다른 요인(YOLO 마스크 해상도 등)일 가능성이 높아짐. 상세는 `4_segmentation.md` 참고.
